document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('.menu'),nav=document.querySelector('.nav nav');if(menu&&nav)menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('.year').forEach(e=>e.textContent=new Date().getFullYear());const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));const form=document.querySelector('#contactForm');if(form){form.addEventListener('submit',async e=>{e.preventDefault();const notice=document.querySelector('.notice'),button=form.querySelector('button');button.disabled=true;button.textContent='Sending…';try{const r=await fetch('contact-form.php',{method:'POST',body:new FormData(form)});const data=await r.json();notice.textContent=data.message;notice.className='notice show';if(data.success)form.reset()}catch{notice.textContent='Unable to send your request right now. Please email hello@quantixglobalsolutions.com.';notice.className='notice show'}finally{button.disabled=false;button.textContent='Send Request'}})}});